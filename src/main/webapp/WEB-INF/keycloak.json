{
  "realm": "${REALM_NAME}",
  "auth-server-url": "${AUTH_SERVER}",
  "ssl-required": "external",
  "resource": "${CLIENT_ID}",
  "enable-basic-auth" : "true",
  "credentials": {
    "secret": "${CLIENT_SECRET}"
  }
}